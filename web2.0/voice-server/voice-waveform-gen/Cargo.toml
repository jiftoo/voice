[package]
name = "voice-waveform-gen"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.5"
# serde = { version = "1.0.194" }
tokio = { version = "1.40.0", features = ["rt", "macros"] }
voice-shared = { path = "../voice-shared" }
tower-http = { version = "0.5.2", features = ["cors", "compression-br"] }
