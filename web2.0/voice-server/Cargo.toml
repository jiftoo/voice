[workspace]
resolver = "2"

members = ["voice-waveform-gen", "voice-shared", "voice-analyzer"]

# faster build times
# [profile.release.package.voice-analyzer]
# opt-level = 0
# debug = false
# 
[profile.release]
strip = true
