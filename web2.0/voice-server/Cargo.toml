[workspace]
resolver = "2"

members = [
	"voice-analyzer",
	"voice-waveform-gen",
	"voice-uri-upload",
	"voice-shared",
]

# faster build times
# [profile.release.package.voice-analyzer]
# opt-level = 0
# debug = false
# 
[profile.release]
strip = true
